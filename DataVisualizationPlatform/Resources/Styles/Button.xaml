<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 设备信息卡片按钮样式 -->
    <Style x:Key="EquipmentCardButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#E0E0E0"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Margin" Value="8"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <!-- 阴影层 -->
                        <Border Name="ShadowBorder" 
                                Background="#10000000"
                                CornerRadius="12"
                                Margin="0,2,2,4"/>

                        <!-- 主要内容层 -->
                        <Border Name="MainBorder" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!-- 鼠标悬停效果 -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="MainBorder"
                                                      Storyboard.TargetProperty="Background.Color"
                                                      To="#F8F9FA" Duration="0:0:0.2"/>
                                        <ColorAnimation Storyboard.TargetName="ShadowBorder"
                                                      Storyboard.TargetProperty="Background.Color"
                                                      To="#20000000" Duration="0:0:0.2"/>
                                        <ThicknessAnimation Storyboard.TargetName="ShadowBorder"
                                                          Storyboard.TargetProperty="Margin"
                                                          To="0,3,3,6" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="MainBorder"
                                                      Storyboard.TargetProperty="Background.Color"
                                                      To="White" Duration="0:0:0.2"/>
                                        <ColorAnimation Storyboard.TargetName="ShadowBorder"
                                                      Storyboard.TargetProperty="Background.Color"
                                                      To="#10000000" Duration="0:0:0.2"/>
                                        <ThicknessAnimation Storyboard.TargetName="ShadowBorder"
                                                          Storyboard.TargetProperty="Margin"
                                                          To="0,2,2,4" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <!-- 按下效果 -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="MainBorder" Property="Background" Value="#E9ECEF"/>
                            <Setter TargetName="ShadowBorder" Property="Background" Value="#05000000"/>
                            <Setter TargetName="ShadowBorder" Property="Margin" Value="0,1,1,2"/>
                        </Trigger>

                        <!-- 禁用状态 -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="MainBorder" Property="Background" Value="#F5F5F5"/>
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="#D6D6D6"/>
                            <Setter TargetName="ShadowBorder" Property="Visibility" Value="Collapsed"/>
                            <Setter Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 设备名称文本样式 -->
    <Style x:Key="EquipmentNameTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="#2C3E50"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>

    <!-- 设备ID文本样式 -->
    <Style x:Key="EquipmentIdTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Foreground" Value="#6C757D"/>
        <Setter Property="Margin" Value="0,0,0,4"/>
    </Style>

    <!-- 设备地址文本样式 -->
    <Style x:Key="EquipmentAddressTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Foreground" Value="#ADB5BD"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <!-- 设备信息图标样式 -->
    <Style x:Key="EquipmentIconStyle" TargetType="Ellipse">
        <Setter Property="Width" Value="40"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Fill" Value="#E3F2FD"/>
        <Setter Property="Margin" Value="0,0,12,0"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
    </Style>

</ResourceDictionary>