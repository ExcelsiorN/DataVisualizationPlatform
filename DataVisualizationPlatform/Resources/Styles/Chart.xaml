<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 颜色定义 -->
    <SolidColorBrush x:Key="PrimaryBlueBrush" Color="#3B82F6"/>
    <SolidColorBrush x:Key="GreenBrush" Color="#10B981"/>
    <SolidColorBrush x:Key="IndigoBrush" Color="#6366F1"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#1F2937"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#6B7280"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#E5E7EB"/>
    <SolidColorBrush x:Key="BackgroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="GridBrush" Color="#F3F4F6"/>
    <SolidColorBrush x:Key="ShadowBrush" Color="#00000018"/>

    <!-- 标题样式 -->
    <Style x:Key="TitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="28"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
    </Style>

    <!-- 图表容器样式 -->
    <Style x:Key="ChartContainerStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
        <!--<Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>-->
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="{Binding Color, Source={StaticResource ShadowBrush}}" 
                                  BlurRadius="10" 
                                  ShadowDepth="2" 
                                  Opacity="0.1"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 图表标题样式 -->
    <Style x:Key="ChartTitleStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="Margin" Value="0,0,0,15"/>
    </Style>

    <!-- 图例文本样式 -->
    <Style x:Key="LegendTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- 图表轴线样式 -->
    <Style x:Key="AxisLineStyle" TargetType="Line">
        <Setter Property="Stroke" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="StrokeThickness" Value="2"/>
    </Style>

    <!-- 网格线样式 -->
    <Style x:Key="GridLineStyle" TargetType="Line">
        <Setter Property="Stroke" Value="{StaticResource GridBrush}"/>
        <Setter Property="StrokeThickness" Value="1"/>
    </Style>

    <!-- 折线样式 -->
    <Style x:Key="LinePathStyle" TargetType="Path">
        <Setter Property="Stroke" Value="{StaticResource PrimaryBlueBrush}"/>
        <Setter Property="StrokeThickness" Value="3"/>
        <Setter Property="Fill" Value="Transparent"/>
        <Setter Property="StrokeLineJoin" Value="Round"/>
        <Setter Property="StrokeStartLineCap" Value="Round"/>
        <Setter Property="StrokeEndLineCap" Value="Round"/>
    </Style>

    <!-- 数据点样式 -->
    <Style x:Key="DataPointStyle" TargetType="Ellipse">
        <Setter Property="Fill" Value="{StaticResource PrimaryBlueBrush}"/>
        <Setter Property="Stroke" Value="White"/>
        <Setter Property="StrokeThickness" Value="2"/>
        <Setter Property="Width" Value="8"/>
        <Setter Property="Height" Value="8"/>
    </Style>

    <!-- 柱状图矩形样式 -->
    <Style x:Key="BarRectangleStyle" TargetType="Rectangle">
        <Setter Property="StrokeThickness" Value="0"/>
    </Style>

    <!-- 轴标签样式 -->
    <Style x:Key="AxisLabelStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- 数值标签样式 -->
    <Style x:Key="ValueLabelStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="9"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FontWeight" Value="Medium"/>
    </Style>

</ResourceDictionary>