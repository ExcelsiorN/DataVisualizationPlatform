<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 基础动画样式 -->
    <Style x:Key="BaseAnimatedStyle" TargetType="RadioButton" BasedOn="{StaticResource PopupStyle}">
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Opacity" Value="0.5"/>
        <!-- 默认透明度50% -->

        <Style.Triggers>
            <!-- 鼠标悬停效果 - 放大并调整为80%透明度 -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="RenderTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="Opacity" Value="0.8"/>
                <!-- 鼠标悬停时50%透明度 -->
            </Trigger>

            <!-- 只有被选中时才完全不透明 -->
            <Trigger Property="IsChecked" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="Opacity"
                                To="1" 
                                Duration="0:0:0.3">
                                <DoubleAnimation.EasingFunction>
                                    <QuadraticEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="Opacity"
                                To="0.5" 
                                Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <QuadraticEase EasingMode="EaseIn"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>

            <!-- 鼠标悬停 + 被选中 = 保持不透明并放大 -->
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsChecked" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Opacity" Value="1"/>
                <Setter Property="RenderTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                    </Setter.Value>
                </Setter>
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <!-- 月份按钮样式 -->
    <Style x:Key="MonthButtonStyle" TargetType="RadioButton" BasedOn="{StaticResource BaseAnimatedStyle}">
        <!-- 继承所有基础动画样式 -->
    </Style>

    <!-- 年份按钮样式 -->
    <Style x:Key="YearButtonStyle" TargetType="RadioButton" BasedOn="{StaticResource BaseAnimatedStyle}">
        <!-- 继承所有基础动画样式 -->
    </Style>

</ResourceDictionary>