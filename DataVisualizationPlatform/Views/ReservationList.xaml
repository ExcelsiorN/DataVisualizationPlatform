<Page x:Class="DataVisualizationPlatform.Views.ReservationList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DataVisualizationPlatform.ViewModels"
             xmlns:controls="clr-namespace:DataVisualizationPlatform.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Style="{StaticResource Page_Style}">

    <Page.Resources>
        <!-- 引用样式资源 -->
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles/ReservationListStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Page.DataContext>
        <vm:ReservationListViewModel />
    </Page.DataContext>

    <ScrollViewer Padding="10" VerticalScrollBarVisibility="Hidden">
        <ItemsControl ItemsSource="{Binding ReservationList}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="White"
                            CornerRadius="12"
                            Padding="20"
                            Margin="10"
                            Width="250"
                            Height="300">
                        <Border.Effect>
                            <DropShadowEffect ShadowDepth="4"
                                        Direction="315"
                                        Color="#40000000"
                                        Opacity="0.3"
                                        BlurRadius="8"/>
                        </Border.Effect>
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick"
                                    Command="{Binding DataContext.FlipCardCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                    CommandParameter="{Binding}" />
                        </Border.InputBindings>

                        <Grid Style="{StaticResource FlipContainerStyle}">
                            <!-- 正面内容 -->

                            <StackPanel Margin="10" 
                                    Visibility="{Binding IsContentFlipped, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Invert}">
                                <Grid Margin="0,0,0,20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="80" />
                                        <!-- 间隔 -->
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <Ellipse Grid.Column="0" Width="10" Height="10"
                                             Fill="{Binding Res_PaymentStatus, Converter={StaticResource StatusToBrushConverter}}"
                                             VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1" Text="{Binding Res_PaymentStatus}" Margin="5,0,0,0" VerticalAlignment="Center" />

                                    <Ellipse Grid.Column="3" Width="10" Height="10"
                                             Fill="{Binding Res_Status, Converter={StaticResource StatusToBrushConverter}}"
                                             VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="4" Text="{Binding Res_Status}" Margin="5,0,0,0" VerticalAlignment="Center" />
                                </Grid>
                                <TextBlock Text="{Binding Res_ID, StringFormat=预约单ID：{0}}" Style="{StaticResource CardItemText}" Margin="0,0,0,5" />
                                <TextBlock Text="{Binding Res_OrderID, StringFormat=下单用户：{0}}" Style="{StaticResource CardItemText}" Margin="0,0,0,5" />
                                <TextBlock Text="{Binding Res_Equipment, StringFormat=设备：{0}}" Style="{StaticResource CardItemText}" Margin="0,0,0,5" />
                                <TextBlock Text="{Binding Res_Date, StringFormat=预约日期：{0}}" Style="{StaticResource CardItemText}" Margin="0,0,0,5" />
                                <TextBlock Text="{Binding Res_Start, StringFormat=预约时段开始：{0}}" Style="{StaticResource CardItemText}" Margin="0,0,0,5" />
                                <TextBlock Text="{Binding Res_End, StringFormat=预约时段结束：{0}}" Style="{StaticResource CardItemText}" Margin="0,0,0,5" />
                                <TextBlock Text="{Binding Res_PaymentInfo, StringFormat=支付信息：{0}}" Style="{StaticResource CardItemText}" Margin="0,0,0,5" />
                                <TextBlock Text="{Binding Res_Detail, StringFormat=预约单详情：{0}}" Style="{StaticResource CardItemText}" Margin="0,0,0,5" />
                                <TextBlock Text="{Binding Res_StartDate, StringFormat=创建日期：{0}}" Style="{StaticResource CardItemText}" Margin="0,0,0,5" />


                            </StackPanel>

                            <!-- 背面内容 -->
                            <StackPanel Margin="10" 
                                Visibility="{Binding IsContentFlipped, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="Test" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>

    
</Page>