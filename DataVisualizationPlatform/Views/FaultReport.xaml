<Page x:Class="DataVisualizationPlatform.Views.FaultReport"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:DataVisualizationPlatform.ViewModels"
      xmlns:controls="clr-namespace:DataVisualizationPlatform.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Style="{StaticResource Page_Style}">

    <Page.Resources>
        <!-- 引用样式资源 -->
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles/FaultReportStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Page.DataContext>
        <vm:FaultReportViewModel />
    </Page.DataContext>

    <ScrollViewer Padding="20" VerticalScrollBarVisibility="Hidden">
        <ItemsControl ItemsSource="{Binding EquipmentList}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource CardStyle}">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" 
                                        Command="{Binding DataContext.FlipCardCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}" />
                        </Border.InputBindings>

                        <Grid Style="{StaticResource FlipZoomContainerStyle}">
                            <!-- 正面内容 -->
                            <StackPanel Margin="10" Panel.ZIndex="1"
                                        Visibility="{Binding IsContentFlipped, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Invert}">
                                <TextBlock Text="{Binding Equ_Name}" Style="{StaticResource CardTitleText}" Margin="0,0,0,20"/>
                                <StackPanel Orientation="Horizontal" Margin="10,5,0,20" HorizontalAlignment="Left">
                                    <Ellipse Width="10" Height="10"
                                           Fill="{Binding Equ_OnlineStatus, Converter={StaticResource StatusToBrushConverter}}"
                                           VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding Equ_OnlineStatus}" Margin="5,0,0,0" />
                                </StackPanel>

                                <TextBlock Text="{Binding Equ_TotalOperationTime, StringFormat=运行时间：{0}}" Style="{StaticResource CardItemText}" />
                                <TextBlock Text="{Binding Equ_UsageRateThisYear, StringFormat=使用率：{0}}" Style="{StaticResource CardItemText}" />
                                <TextBlock Text="{Binding Equ_UsedFixedDurationThisYear, StringFormat=已用固定时长：{0}}" Style="{StaticResource CardItemText}" />
                                <TextBlock Text="{Binding Equ_FixedDurationThisYear, StringFormat=总固定时长：{0}}" Style="{StaticResource CardItemText}" />
                                <TextBlock Text="{Binding Equ_AvailableBookingPeriod, StringFormat=预约配置：{0}}" Style="{StaticResource CardItemText}" />
                                <TextBlock Text="{Binding Equ_DeploymentAddress, StringFormat=地址：{0}}" Style="{StaticResource CardItemText}" />
                            </StackPanel>

                            <!-- 背面内容 -->
                            <StackPanel Margin="20" 
                                        Visibility="{Binding IsContentFlipped, Converter={StaticResource BooleanToVisibilityConverter}}">

                                <TextBlock Text="{Binding Equ_Name}" Style="{StaticResource BackTitleText}" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</Page>